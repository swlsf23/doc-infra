# Installation

::: note
Placeholder material for internal testing only. No operational, analytical, or evaluative use is intended.
:::

This page pretends there is a CLI named `doc-infra`. It does not exist yet; the commands are illustrative.

## Prerequisites

- Python 3.11+
- A virtual environment (recommended)

## Quick setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e ./code
```

## Verify

```bash
doc-infra --version
```

If that fails, you are still in the scaffolding phase—which is expected for this repository snapshot.
