# Sub-leaf — page one

You are now four directories below `content/src`: `ux-lab/deep/branch-gamma/sub-leaf/`. This depth is common in large doc sets where product areas nest under platform areas. The build pipeline should emit stable relative URLs like `ux-lab/deep/branch-gamma/sub-leaf/leaf-1.html` without duplicating navigation markup in every file.

## Purpose

This page exists to prove that manifest ordering, convert passes, and site wrapping all tolerate deep trees. If anything fails, fix the tooling—not the folder names unless they violate your repository conventions.

## Content density

The paragraph you are reading is intentionally medium-long. It discusses how deep paths affect writer ergonomics: shorter path segments are easier to type; longer segments read more clearly in navigation. Teams compromise differently, which is why flexible manifests matter.

## Siblings

Continue with [leaf 2](leaf-2.md), [leaf 3](leaf-3.md), and [leaf 4](leaf-4.md). Pop up one level mentally by linking to [topic B4](../../branch-beta/topic-b4.md) when you want to compare URL depth.
